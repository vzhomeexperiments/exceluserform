Option Explicit

'========================================
'Launch the userform
'========================================
' This program is written to allow UserForm launch from a button
Sub StartUF()

' specify True if we can not modify Workbook while UserForm is open
' specify False if we can modify Workbook while UserForm is open
MyCarCheckListFormR.Show False

' protect worksheet from writing (can be still unprotected
Worksheets("Report").Protect , UserInterfaceOnly:=True
' this will also protect with password
' Worksheets("Report").Protect "Password", UserInterfaceOnly:=True

   
End Sub


